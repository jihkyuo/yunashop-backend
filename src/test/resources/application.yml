# spring:
    # 테스트 환경에서는 스프링 부트가 기본적으로 자동으로 메모리 DB를 사용한다. => 따라서 아래 설정일 필요없다
    # datasource:
    #   url: jdbc:h2:mem:test
    #   username: sa
    #   password:
    #   driver-class-name: org.h2.Driver

    # jpa:
    #   hibernate:
    #     ddl-auto: create
    #   properties:
    #     hibernate:
    #       # show_sql: true
    #       format_sql: true

logging:
    level:
        org.hibernate.SQL: debug
        org.hibernate.type: trace
